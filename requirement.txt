# 核心依赖
PyQt5>=5.15.0
requests>=2.25.0
urllib3>=1.26.0

# 文档处理依赖
PyMuPDF>=1.23.0  # 对应代码中的fitz模块
python-docx>=0.8.11
pandas>=1.3.0
openpyxl>=3.0.7  # pandas处理Excel需要的依赖
comtypes>=1.1.10  # Windows系统下Word转PDF需要

# 图像处理依赖
Pillow>=8.2.0

# Web解析依赖
beautifulsoup4>=4.9.3
lxml>=4.6.3  # BeautifulSoup4的XML解析器

# 可选依赖（用于打包）
PyInstaller==5.13.0
pefile==2023.2.7

# 其他辅助库
retry>=0.9.2  # 用于处理网络请求重试
chardet>=4.0.0  # 字符编码检测
python-dateutil>=2.8.1  # 日期时间处理
numpy>=1.19.5  # 数值计算，pandas的依赖